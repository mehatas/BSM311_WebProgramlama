@model eBiletApp.Models.Sinema
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer
@{
    ViewData["Title"] = "Sinema Hakkında";
}

<h1 class="text-center>@localizer["Sinema Hakkında"]</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2 align-self-center">
            @Html.DisplayNameFor(model => model.SinemaFotografi)
        </dt>
        <dd class="col-sm-10 pb-5">
            <img class="w-50 h-100" src="@Model.SinemaFotografi" asp-append-version="true">
        </dd>
        <dt class="col-sm-2 pb-5 align-self-center">
            @Html.DisplayNameFor(model => model.SinemaAdi)
        </dt>
        <dd class="col-sm-10 pb-5">
            @Html.DisplayFor(model => model.SinemaAdi)
        </dd>
        <dt class="col-sm-2 align-self-center">
            @Html.DisplayNameFor(model => model.SinemaHakkinda)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.SinemaHakkinda)
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
    {<a class="btn btn-outline-light text-dark" asp-action="Edit" asp-route-id="@Model.SinemaId">@localizer["Düzenle"]</a>}
<a class="btn btn-secondary ml-3" asp-action="Index">@localizer["Geri Dön"]</a>
</div>
